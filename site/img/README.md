# site/img

`site/index.html` と `site/en/index.html` が参照する画像を置きます。

| ファイル | 写すもの |
|---|---|
| `package-manager.png` | Unity の Package Manager の一覧に Unity MCP が追加された状態。左の一覧で Unity MCP が選ばれ、バージョンが読める大きさで写っていること。 |
| `preferences.png` | Edit > Preferences の Unity MCP ページを英語で表示し、セットアップの一番上が `✓ Listening on port <番号>` になっている状態。設定とヘルプは閉じたまま撮る。 |
| `preferences-ja.png` | 同じページを日本語表示にしたもの。`site/index.html` が参照する。 |
| `releases-assets.png` | GitHub の Releases ページの Assets 一覧。`isuzu-unity-cli.mcpb` の行が読める大きさで写っていること。ヘッドレスの Chrome で撮れる（`--virtual-time-budget` を付けないと一覧が読み込まれる前に撮れる）。 |

## まだ無い画像

手順 A の 4 番目と 5 番目には図がありません。枠だけのプレースホルダーを置くより、文章だけのほうが読みやすいと判断したためです。どちらもキャプションに書いていた内容が本文に入っているので、図が無くても手順は通ります。

足すなら次の内容になります。どちらも Claude Desktop の画面なので、画面の読み取りになります。手前に別のアプリケーションがあるとそれが写るので、撮る前に片付けてください。

| 足すとしたら | 写すもの |
|---|---|
| 手順 A の 4 番目 | Claude Desktop が拡張機能を導入するときの確認画面。Unity project name の入力欄が読めること。 |
| 手順 A の 5 番目 | Unity のシーンについてやり取りしている会話。1 つの例が典型例に見えるので、載せるかどうかから判断すること。 |
