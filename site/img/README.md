# site/img

`site/index.html` と `site/en/index.html` が参照する画像を置きます。

| ファイル | 写すもの |
|---|---|
| `package-manager.png` | Unity の Package Manager の一覧に Unity MCP が追加された状態。左の一覧で Unity MCP が選ばれ、バージョンが読める大きさで写っていること。 |
| `preferences.png` | Edit > Preferences の Unity MCP ページを英語で表示し、セットアップの一番上が `✓ Listening on port <番号>` になっている状態。設定とヘルプは閉じたまま撮る。 |
| `preferences-ja.png` | 同じページを日本語表示にしたもの。`site/index.html` が参照する。 |

次の 3 つは最初のリリースまで存在しないので、HTML 側では枠だけのプレースホルダーを描いています。差し替え箇所は `<!-- TODO: replace after the first release -->` で探せます。

- GitHub Releases の Assets に `isuzu-unity-cli.mcpb` が並んでいる画面
- Claude Desktop が拡張機能を導入するときの確認画面
- Claude Desktop で Unity のシーンについてやり取りしている会話の画面
